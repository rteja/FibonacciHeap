#include <stdlib.h>
#include "fibonacciheap.h"

int main()
{
     FibonacciHeap heap;
     heap.Insert(1, NULL);
     heap.Insert(10, NULL);
     heap.Insert(2, NULL);
     heap.Insert(3, NULL);

     return 0;
}
